apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cyberark-demo

resources:
  - namespace.yaml
  - rbac/namespace-access.yaml
  - ../../operators/redis
  - ../../operators/external-secret-operator
  - ../../chain/chain-1


patchesStrategicMerge:
  - patch-dspa.yaml
  - og-patch.yaml
