apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-providers
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:

      - name: vllm
        folder: vLLM
        type: file
        options:
          path: /etc/grafana/provisioning/dashboards/vllm
